<section class="timeline">
  <% form_tag timeline_path do %>
    <%= text_area_tag :message, nil, :placeholder => 'Message here.' %>
    <%= text_field @created_time, nil, :placeholder => 'Enter time' %>
    <%= submit_tag 'Update', :disable_with => 'Updating...' %>
  <% end %>
  <% @posts.each do |post| %>
    <article class="clearfix">
      <div class="avatar">
        <fb:profile-pic uid="<%= post.from.identifier %>" size="square"></fb:profile-pic>
      </div>
      <p>
        <strong><fb:name uid="<%= post.from.identifier %>"></fb:name></strong>
        <%= post.message || post.name || post.caption || post.description || post.created_time %>
      </p>
    </article>
  <% end %>
</section>